__pycache__/
parent_check.db
*.pyc
*.joblib

# Secrets — never commit. Only .env.example is tracked.
.env

# Nested prompt-evaluation working copy (its own repo + .env secrets) — never commit
prompt test/

# local agent/editor config — never commit
.claude/
